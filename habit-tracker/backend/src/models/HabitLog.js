class HabitLog {

    constructor(
        id,
        habitId,
        completedAt
    ) {
        this.id = id;
        this.habitId = habitId;
        this.completedAt = completedAt;
    }

}

module.exports = HabitLog;